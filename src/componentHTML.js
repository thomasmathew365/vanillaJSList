
export const modalHTML = (id) => `
    <div class="absolute w-full h-96 max-w-md bg-purple-300 opacity-100 my-40 mx-auto z-20 flex items-center justify-center" data-list="modal">Pop-up view for ${id}</div>
`;
export const listContent = (id) => `
<span>${id}</span>
`;